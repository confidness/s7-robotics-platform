# PIN required to create a mentor account. Set this in Vercel → Settings → Environment Variables.
# Without it the app falls back to a development PIN, which is fine locally and wrong in production.
VITE_MENTOR_PIN=4821
